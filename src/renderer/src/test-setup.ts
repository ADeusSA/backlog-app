/**
 * Общая настройка тестов renderer: матчеры jest-dom (`toHaveTextContent`, `toBeVisible`…)
 * и их типы для vitest. Подключается через `setupFiles` в `vitest.config.ts`.
 */
import '@testing-library/jest-dom/vitest'
